'use client';
import { useState } from 'react';
export function InteractiveExercise(){const[done,setDone]=useState(false);const[revealed,setRevealed]=useState(false);return <div className="glass rounded-3xl p-6"><h3 className="text-xl font-bold">Actividad interactiva</h3><p className="mt-2 text-zinc-300">Selecciona una acción mínima para aplicar hoy.</p><div className="mt-4 grid gap-2">{['Dos minutos de lectura','Preparar mi entorno','Escribir una reflexión'].map(x=><label className="rounded-2xl bg-white/5 p-3" key={x}><input type="radio" name="act"/> {x}</label>)}</div><textarea className="mt-4 w-full rounded-2xl bg-white/5 p-3" placeholder="Mi reflexión..."/><button className="mt-3 rounded-xl border border-white/15 px-4 py-2" onClick={()=>setRevealed(!revealed)}>Revelar escenario</button>{revealed&&<p className="mt-3 rounded-2xl bg-violet-500/10 p-3">Escenario: si tienes poco tiempo, reduce la acción hasta que sea imposible fallar.</p>}<button className="mt-4 rounded-xl bg-white px-4 py-2 font-bold text-black" onClick={()=>setDone(true)}>{done?'Completado':'Marcar como completado'}</button></div>}
