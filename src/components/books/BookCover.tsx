import type { Book } from '@/types';
import { BarChart3, Bolt, Brain, Clock, Gem, Leaf, Orbit, Shield, Stairs, Users } from 'lucide-react';
const icons={orbit:Orbit,stairs:Stairs,bolt:Bolt,chart:BarChart3,mind:Brain,clock:Clock,people:Users,leaf:Leaf,diamond:Gem,shield:Shield};
export function BookCover({book,size='md'}:{book:Book;size?:'sm'|'md'|'lg'}){const Icon=icons[book.coverConfig.symbol];const sizes={sm:'h-44 w-32',md:'h-64 w-44',lg:'h-80 w-56'};return <div className={`${sizes[size]} relative shrink-0 overflow-hidden rounded-3xl border border-white/15 bg-gradient-to-br ${book.coverConfig.gradient} p-4 shadow-2xl`}><div className="absolute inset-0 bg-black/35"/><div className="absolute -right-8 -top-8 h-28 w-28 rounded-full bg-white/20 blur-2xl"/><div className="relative z-10 flex h-full flex-col justify-between"><div><p className="text-xs uppercase tracking-[.25em] text-white/70">Ceoteca demo</p><h3 className="mt-5 text-xl font-black uppercase leading-tight">{book.title}</h3><p className="mt-2 text-sm text-white/75">{book.author}</p></div><Icon className="mx-auto h-20 w-20 drop-shadow-[0_0_20px_rgba(255,255,255,.45)]"/><p className="rounded-full bg-white/15 px-3 py-1 text-xs">{book.readingTime} min · {book.category}</p></div></div>}
