'use client';
import { Menu, X } from 'lucide-react';import Link from 'next/link';import { useState } from 'react';import { publicNavigation } from '@/config/navigation';import { Logo } from '@/components/ui/Logo';import { GradientButton } from '@/components/ui/Buttons';
export function PublicHeader(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-white/10 bg-[#05050a]/75 backdrop-blur-xl"><div className="container flex h-20 items-center justify-between"><Logo/><nav className="hidden items-center gap-8 text-sm md:flex">{publicNavigation.map(i=><Link className="focus-ring text-zinc-200 hover:text-white" key={i.href} href={i.href}>{i.label}</Link>)}</nav><div className="hidden items-center gap-4 md:flex"><Link className="focus-ring text-sm" href="/login">Iniciar sesión</Link><GradientButton href="/registro">Empieza gratis</GradientButton></div><button className="focus-ring md:hidden" onClick={()=>setOpen(!open)} aria-expanded={open} aria-label="Abrir menú">{open?<X/>:<Menu/>}</button></div>{open&&<div className="container grid gap-3 pb-6 md:hidden">{publicNavigation.map(i=><Link key={i.href} href={i.href} onClick={()=>setOpen(false)}>{i.label}</Link>)}<Link href="/login">Iniciar sesión</Link><GradientButton href="/registro">Empieza gratis</GradientButton></div>}</header>}
