import { Check } from 'lucide-react';import { plans } from '@/config/plans';import { GradientButton, SecondaryButton } from '@/components/ui/Buttons';
export function PricingCards(){return <div className="grid gap-5 lg:grid-cols-4">{plans.map(p=><article key={p.id} className={`glass rounded-3xl p-6 ${p.highlighted?'ring-2 ring-violet-400':''}`}><p className="text-sm font-bold text-violet-300">{p.name}</p><h3 className="mt-3 text-4xl font-black">${p.monthlyPrice}<span className="text-base text-zinc-400">/mes</span></h3>{p.badge&&<span className="mt-2 inline-block rounded-full bg-pink-500/20 px-3 py-1 text-xs text-pink-200">{p.badge}</span>}<p className="mt-3 text-sm text-zinc-300">{p.description}</p><ul className="my-6 space-y-3 text-sm">{p.features.map(f=><li className="flex gap-2" key={f}><Check className="h-4 w-4 text-green-400"/>{f}</li>)}</ul>{p.highlighted?<GradientButton href={`/registro?plan=${p.id}`}>{p.cta}</GradientButton>:<SecondaryButton href={`/registro?plan=${p.id}`}>{p.cta}</SecondaryButton>}</article>)}</div>}
