# Roles editoriales

Admin administra y publica; editor crea, edita y envía; reviewer revisa y puede publicar; viewer solo consulta y previsualiza. Los roles se resuelven en servidor desde `training_editorial_users` y nunca desde el cliente.
